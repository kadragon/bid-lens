# 로컬 개발용 secret. 복사: cp .dev.vars.example .dev.vars 후 값 채우기.
# .dev.vars 는 .gitignore 포함 — 커밋 금지.
OPEN_DATA_API_PROXY_URL=<proxy-base-url>
OPEN_DATA_X_API_KEY=<x-api-key>
# /admin Basic Auth 비밀번호 (username 고정 "admin"). 미설정 시 /admin 503.
ADMIN_PASSWORD=<your-admin-password>
