# Dependencies
node_modules/

# Build / runtime artifacts
dist/
.wrangler/

# Lockfiles (large, low signal)
pnpm-lock.yaml

# Secrets — never scan
.dev.vars
.dev.vars.*

# Workspace config (local-only)
pnpm-workspace.yaml
